//! skills-provider-conda — `SkillRegistry` implementation for conda channels.
//!
//! The first-class provider: repodata fetching, `skill-*` package resolution,
//! and extraction happen through the rattler crates.
//!
//! See `.knowledge/crates/skills-provider-conda.md` for the full design.
